import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as a,c as i,e as r}from"./app-706bfe03.js";const t={},h=r('<h1 id="资源加载和网络栈" tabindex="-1"><a class="header-anchor" href="#资源加载和网络栈" aria-hidden="true">#</a> 资源加载和网络栈</h1><h2 id="webkit资源加载机制" tabindex="-1"><a class="header-anchor" href="#webkit资源加载机制" aria-hidden="true">#</a> webkit资源加载机制</h2><h3 id="资源" tabindex="-1"><a class="header-anchor" href="#资源" aria-hidden="true">#</a> 资源</h3><p>网络和资源加载时网页的加载和渲染过程的第一步，也是必不可少的一步。网页本身就是一种资源，网页还会依赖很多其他类型的资源，比如图片、视频等。</p><p>HTML 支持的资源主要包括以下类型：</p><ul><li>HTML：包括各种 HTML 元素</li><li>JavaScript：JavaScript 代码，可以内嵌在 HTML 文件中，也可以是单独文件</li><li>CSS：同样可内嵌可单独文件</li><li>图片：各种编码格式的图片，包括 png、jpeg 等</li><li>SVG：用于绘制 SVG 的 2D 矢量图形</li><li>css shader：css shader 文件</li><li>视频、音频、字幕：多媒体资源</li><li>字体文件：css 支持自定义字体</li><li>XSL 样式表： 使用 XSLT 语言编写的 XSLT 代码文件</li></ul><p>以上资源在 webkit 中均有不同的类表示，它们的公共基类是 <em>CachedResource</em>。</p><p><em>HTML 文本在 webkit 的类型叫 MainResource 类，对应的资源类是 CachedRawResource 类。</em></p><p><img src="https://raw.githubusercontent.com/yamsfeer/pic-bed/master/008i3skNgy1gv06l2eyayj60cp06rq3b02.jpg" alt=""></p><p><em>资源类有 &quot;Cached&quot; 字样是因为缓存机制，所有对资源的请求都会先获取缓存中的信息，以决定是否向服务器发起资源请求。</em></p><h3 id="资源缓存" tabindex="-1"><a class="header-anchor" href="#资源缓存" aria-hidden="true">#</a> 资源缓存</h3><p>资源的缓存机制是提高资源使用效率的有效方法。</p><p>它的基本思想是建立一个资源的缓存池，当需要请求资源时，先从资源池中查找是否存在相应资源。如果有，则取出使用；如果没有，webkit 创建一个新的 <em>CachedResource</em> 的子类对象，并发送请求给服务器，webkit 接收资源后将其设置到对应的资源类对象中。</p><div class="hint-container tip"><p class="hint-container-title">Tips</p><p>这里的缓存指的是<strong>内存缓存</strong>，而不同于后面网络栈部分的磁盘缓存。</p></div><img src="https://raw.githubusercontent.com/yamsfeer/pic-bed/master/008i3skNgy1gv07f5zv9xj60ck0793ym02.jpg" style="zoom:90%;"><p>Webkit 从资源池中查找资源的关键字是 URL，因为标记资源唯一性的特征就是 URL。因此，即使两个资源的内容完全一样而 URL 不同，也会被认为是两个不同的资源。</p><p>事实上，真实的资源缓存远比上图复杂，这其中涉及了资源的生命周期和失效机制。</p><h3 id="资源加载器" tabindex="-1"><a class="header-anchor" href="#资源加载器" aria-hidden="true">#</a> 资源加载器</h3><p>按加载器的类型来分，webkit 共有3种类型的加载器。</p><ul><li><p>第一种，针对资源类型的特定加载器</p><p>其特点是仅加载某一种资源。例如 &quot;image&quot; 元素的资源加载器是 <em>ImageLoader</em> 类，自定义字体对应的资源加载器为 <em>FontLoader</em> 类。</p><p>这种加载器没有公共基类。</p></li><li><p>第二种，缓存资源加载器：<em>CachedResourceLoader</em> 类</p><p>特定加载器会先通过缓存资源加载器来检查是否有缓存资源。</p></li><li><p>第三种，通用资源加载器：<em>ResourceLoader</em> 类</p><p>当 webkit 需要从网络或文件系统获取资源时使用。</p></li></ul><h3 id="资源加载过程" tabindex="-1"><a class="header-anchor" href="#资源加载过程" aria-hidden="true">#</a> 资源加载过程</h3><p>以加载图片为例，当 HTML 解释器解析到 <code>img</code> 元素时，webkit 会创建一个 <em>ImageLoader</em> 对象来加载资源。</p><p><em>ImageLoader</em> 创建加载资源请求的过程如下图：</p><img src="https://raw.githubusercontent.com/yamsfeer/pic-bed/master/008i3skNgy1gv1g6hmqfrj60ei0bfjs502.jpg" style="zoom:85%;"><p>图中到 <em>CachedResourceLoader</em> 这一步时，如果缓存池中已有该图片资源（图片URL相同），则使用缓存池中的对象；否则，创建一个资源请求 <em>ResourceRequest</em>。</p><p>从网络获取资源是非常耗时的，因此通常资源的加载是异步执行的，也就是说资源的获取和加载<strong>不会阻碍</strong>当前 webkit 的渲染过程，例如图片、css 文件。</p><p>需要说明的是，JavaScript 代码文件的加载是会阻碍主线程的渲染过程的。</p><p>为了解决这个问题，webkit 的做法是：</p><p>当主线程被阻碍是，webkit 会<strong>启动另外一个线程</strong>去遍历后面的 HTML 内容，收集需要的资源 URL，然后发送请求。与此同时，webkit 能够并发下载这些资源，甚至并发下载 JavaScript 代码资源。</p><p><strong>这种机制对于网页的加载提速非常明显</strong>。</p><h3 id="资源的生命周期" tabindex="-1"><a class="header-anchor" href="#资源的生命周期" aria-hidden="true">#</a> 资源的生命周期</h3><p>资源池的大小不是无限的，资源池淘汰过期资源的策略是采用 <em><strong>LRU(Least Recent Used 最近最少使用) 算法</strong></em>。</p><p>那么 webkit 如何判断是否需要从服务器重新请求来更新资源呢？</p><p>webkit 的做法是：首先判断资源是否在资源池中，如果是，发送一个 HTTP 请求给服务器，服务器判断资源是否有更新，如果没有，服务器返回状态码304，表示无需更新，可以直接利用资源池中的资源；否则，webkit 申请下载最新的资源内容。</p><h2 id="chromium多进程资源加载" tabindex="-1"><a class="header-anchor" href="#chromium多进程资源加载" aria-hidden="true">#</a> Chromium多进程资源加载</h2><h3 id="多进程" tabindex="-1"><a class="header-anchor" href="#多进程" aria-hidden="true">#</a> 多进程</h3><h3 id="工作方式和资源共享" tabindex="-1"><a class="header-anchor" href="#工作方式和资源共享" aria-hidden="true">#</a> 工作方式和资源共享</h3><h2 id="网络栈" tabindex="-1"><a class="header-anchor" href="#网络栈" aria-hidden="true">#</a> 网络栈</h2><h3 id="webkit的网络设施" tabindex="-1"><a class="header-anchor" href="#webkit的网络设施" aria-hidden="true">#</a> webkit的网络设施</h3><p>由第三章的 webkit 架构图可知，Webkit 的资源加载是由各个移植来实现的。</p><p>从 webkit 的代码结构可以看出，网络部分的代码是很少的，它们都在目录 <em>&quot;WebKit/Source/WebCore/platform/network&quot;</em> 中。主要是一些 HTTP 消息头、MIME 消息、状态码等信息的描述和处理，没有实质的网络连接和针对网络的优化。</p><h3 id="chromium网络栈" tabindex="-1"><a class="header-anchor" href="#chromium网络栈" aria-hidden="true">#</a> Chromium网络栈</h3><h3 id="磁盘本地缓存" tabindex="-1"><a class="header-anchor" href="#磁盘本地缓存" aria-hidden="true">#</a> 磁盘本地缓存</h3><h3 id="cookit机制" tabindex="-1"><a class="header-anchor" href="#cookit机制" aria-hidden="true">#</a> Cookit机制</h3><h3 id="安全机制" tabindex="-1"><a class="header-anchor" href="#安全机制" aria-hidden="true">#</a> 安全机制</h3><h3 id="高性能网络栈" tabindex="-1"><a class="header-anchor" href="#高性能网络栈" aria-hidden="true">#</a> 高性能网络栈</h3>',46),d=[h];function s(c,o){return a(),i("div",null,d)}const m=e(t,[["render",s],["__file","4. 资源加载和网络栈.html.vue"]]);export{m as default};
